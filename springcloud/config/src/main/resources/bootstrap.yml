spring: 
  cloud: 
    config: 
      name: test
      label: master
      discovery:
        enabled: true
        #配置中心spring.application.name指定的名字
        service-id: config
        
eureka: 
  client: 
    service-url: 
      defaultZone: http://admin:admin123@localhost:8101/eureka/
